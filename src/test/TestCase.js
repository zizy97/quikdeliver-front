// import React from 'react'
// import { useState } from 'react';
// import SocketBuilder from '../api/socket/socket'

export default function TestCase() {
  // var ob = new SocketBuilder();
  // const [connected,setConnected] = useState(false);
  // const [message,setMessage] = useState({});
  return (
    <>
        {/* {connected?"Connected":"Note Connected"}<br/>
        <button onClick={()=>{ob.connect(setConnected,setMessage)}}>Clickme</button>
        <br/>
        <button onClick={()=>{ob.sendName("Supun")}}>Send</button>
        <br/>
        Message-{message.message} */}
    </>
  )
}
